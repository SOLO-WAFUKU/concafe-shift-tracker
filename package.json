{
  "name": "concafe-shift-tracker",
  "version": "1.0.0",
  "description": "ConCafe Shift Tracker",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {}
}